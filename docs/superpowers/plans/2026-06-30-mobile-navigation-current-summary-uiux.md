# Mobile Navigation Current Summary UIUX Plan

## Stage 3 Closed Loop

1. Add failing tests for the summary helper and mobile header source contract.
2. Implement a route-context-backed mobile navigation summary helper.
3. Render the current-location summary in the expanded mobile menu and add an accessible nav label.
4. Run focused tests, lint, full tests, build, and diff hygiene checks.
5. Restart the local production server if needed and verify the mobile menu in the in-app Browser at 390 x 844.
6. Commit, push `main`, wait for GitHub Actions, confirm Vercel production deployment, run a live mobile browser check, and record evidence in `.agent` logs.
