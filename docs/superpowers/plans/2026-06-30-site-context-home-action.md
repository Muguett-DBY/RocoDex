# Site Context Home Action Plan

## Stage 6 Closed Loop

1. Add failing tests for the helper and context strip integration.
2. Implement the home action helper and render it in `SiteContextBar`.
3. Run focused tests, diff hygiene, lint, full tests, and build.
4. Verify locally on desktop and mobile production browser views.
5. Commit, push `main`, wait for GitHub Actions, confirm Vercel, run live verification, and update final logs.
